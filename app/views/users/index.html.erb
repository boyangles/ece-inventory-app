<% provide(:title, 'All Users') %>

<div class="center_jumbotron">
  <h2 class="page-header">Users
    <% if is_admin? %>
      <%= link_to "Create New Account", new_user_url, :class => "btn btn-primary pull-right" %>
    <% end %>
  </h2>
</div>

<%= will_paginate %>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th></th>
      <th>Username</th>
      <th>Privilege</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render @users %>
  </tbody>
</table> 



<%= will_paginate %>
