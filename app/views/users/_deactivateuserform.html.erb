<%= form_for(user) do |f| %>
  <%= f.hidden_field :status, :value => "deactivated" %>
  <%= f.submit "Delete", data: {confirm: 'Are you sure you want to deactivate user ' + user.username + '?'}, class: 'btn btn-primary' %>
<% end %>
