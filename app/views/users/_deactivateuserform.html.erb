<%= form_for(user) do |f| %>
  <%= f.hidden_field :status, :value => "deactivated" %>
	<%= button_tag "", type: "submit", class: "btn btn-danger pull-right glyphicon glyphicon-trash", data: {confirm: 'Are you sure you want to deactivate user ' + user.username + '?'} %>
<% end %>
