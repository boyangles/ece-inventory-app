<%= form_for(request), :url => {:action => :create_new_cart} do |f| %>
  
  <%= f.hidden_field :status, :value => "outstanding" %>

	<div class="actions">
  	<%= f.submit "Place Order" %>
	</div>
<% end %>
