<% if is_manager_or_admin? %>
    <h1>Editing Item</h1>

    <%= render 'form', item: @item %>

    <%= link_to 'Show', @item %> |
    <%= link_to 'Back', items_path %>
<% else %>
    <%= controller.redirect_to items_path %>
<% end %>
