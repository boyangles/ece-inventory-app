<%= form_for(item) do |f| %>
  <%= f.hidden_field :status, :value => "deactive" %>
	<%= button_tag "", type: "submit", class: "btn btn-danger glyphicon glyphicon-trash", data: {confirm: "Are you sure you want to delete this?"} %>

<% end %>
