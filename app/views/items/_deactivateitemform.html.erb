<%= form_for(item) do |f| %>
  <%= f.hidden_field :status, :value => "deactive" %>
	<%= f.submit "Delete", data: {confirm: 'Are you sure you want to delete this item?'}, class: 'btn btn-primary' %>
<% end %>
