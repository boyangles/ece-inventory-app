<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "ECE INVENTORY", root_path, class: "navbar-brand" %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li><%= link_to "Items", items_path %></li>
            <% if is_manager_or_admin? %>
                <li><%= link_to "Users", users_path %></li>
            <% end %>


            <li><%= link_to "My Cart", request_path(grab_cart(current_user).id) %></li>

            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">More...<span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <% if is_manager_or_admin? %>
                    <li><%= link_to "Logs", logs_path %></li>
                    <li><%= link_to "Tags", tags_path %></li>
              <% end %>
                <% request_label = (is_manager_or_admin?) ? "Requests" : "My Requests" %>
                <li><%= link_to request_label, requests_path %></li>
                <% loans_label = (is_manager_or_admin?) ? "Loans" : "My Loans" %>
                <li><%= link_to loans_label, loans_index_path %></li>
              </ul>
            </li>

            <li class="dropdown">
              <a href="#" id="my-account" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">My Account<span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li><%= link_to "API Debugger", auth_token_user_path(current_user) %> </li>

                <% if is_manager_or_admin? %>
                    <li><%= link_to "Bulk Import", import_upload_path %></li>
                <% end %>
                <li class="divider"></li>
                <li><%= link_to "Log out", logout_path, method: "delete" %></li>
              </ul>
            </li>

        <% end %>

      </ul>
    </div>
  </div>
</nav>
