<%= form_for [request_item, request_item.request_item_comments.new], remote: true do |f| %>
  <%= f.text_field :comment, placeholder: "Add a comment here...", id: "request_item_comments_comment_#{request_item.id}" %>
    <%= f.submit "Submit Comment" %>
<% end %>